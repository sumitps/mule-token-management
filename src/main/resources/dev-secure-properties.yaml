keystore:
  password: "<keystorepass>"

alation:
  #replace these values
  username: "<username>"
  password: "<password>"
  http:
    host: "<alation-host>"
    port: "443"
  paths:
    accessToken: "/integration/v1/createAPIAccessToken/"
    refreshToken: "/integration/v1/createRefreshToken"
    asyncUpdate: "/integration/v2/custom_field_value/async/"
  #replace this value
  userId: "31"